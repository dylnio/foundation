{% for row in section %}
    <div class="redis dt">
        <div class="command">{{ row.command }}(<span class="query">{{ row.query }}</span>)</div>
        {% if row.backtrace is defined %}
            <div>
                <div><a class="_bttoggler">backtrace</a></div>
                <div class="backtrace" style="display: none;">
                    <ul>
                        {% for bt in row.backtrace %}
                            <li>→ {{ bt.file }}:{{ bt.line }} {{ bt.function }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        {% endif %}
        <div class="time">{{ row.time }} secs.</div>
    </div>
{% endfor %}
